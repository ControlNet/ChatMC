
- Existing config plumbing is TOML-based (`llm.toml`) and intentionally forgiving on parse failures; MCP config must be strict JSON instead, so do not mirror the silent-fallback behavior for invalid shared MCP configs.
- The new MCP file is locked to `config/mineagent/mcp.json` with only shared `mcpServers` content; reject MineAgent-only fields and non-shared auth/header fields during validation.
- Required Gradle acceptance commands are currently blocked by pre-existing non-MCP compile failures: `:base:core:compileTestJava` cannot resolve existing session/proposal symbols in `core/session` tests, and `:base:common-1.20.1:compileJava` cannot resolve multiple existing `space.controlnet.mineagent.core.*` symbols before reaching the new MCP loader test.
- Earlier task-1 verification noise was misleading: after fixing Gson scope and rerunning the exact task-1 commands sequentially, both MCP acceptance commands passed cleanly.
- /home/zhixi/GitRepos/MineAgent/base/core/src/main/java/space/controlnet/mineagent/core/agent/AgentLoop.java: direct runtime reflection against `AgentLoop` inside isolated `:base:core:test` regressions can trip `NoClassDefFoundError` on `org.bsc.langgraph4j.state.AgentState`, so prompt-contract regressions are safer as source-contract assertions unless the full agent graph runtime is on the test classpath.
- Running `./gradlew --no-daemon --configure-on-demand :base:common-1.20.1:test --tests 'space.controlnet.mineagent.common.agent.ThreadConfinementRegressionTest'` and `./gradlew --no-daemon --configure-on-demand :base:common-1.20.1:test --tests 'space.controlnet.mineagent.common.tools.McpSchemaMappingRegistrationRegressionTest'` in parallel can block briefly on the shared Fabric Loom cache lock; both commands still passed, but serial execution is less noisy if lock-wait output matters.
- Java LSP initialization timed out repeatedly in this workspace during task-5 verification even after the modified MCP files compiled and `McpStdioClientLifecycleRegressionTest` passed, so Gradle compile/test output was the only reliable verification signal for the new stdio transport code in this session.
