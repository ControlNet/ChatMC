
- /home/zhixi/GitRepos/ChatMC/base/core/src/main/java/space/controlnet/chatmc/core/agent/LlmConfig.java: repo convention is a small immutable record with `defaults()` for shared config state.
- /home/zhixi/GitRepos/ChatMC/base/core/src/main/java/space/controlnet/chatmc/core/agent/LlmConfigParser.java and LlmConfigValidator.java: keep parsing/validation split; parser returns defaults on bad input, validator returns a `List<String>` of stable user-facing errors.
- /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/llm/LlmConfigLoader.java: loader creates `config/chatmc` and writes defaults when absent, then delegates parse + validation.
- For strict MCP JSON, preserving insertion order with `LinkedHashMap` plus immutable wrappers keeps written `mcp.json` output and validation ordering deterministic for regression tests.
- `base/core` executes Gson in main code (`McpConfigParser`), so `compileOnly` is not enough there; use a runtime scope (`implementation`) so core JUnit runs do not fail with `NoClassDefFoundError`.
- Duplicate MCP aliases must be rejected during streaming parse with `JsonReader`; parsing through `JsonObject` first silently overwrites repeated names and loses the regression signal.
- /home/zhixi/GitRepos/ChatMC/base/core/src/main/java/space/controlnet/chatmc/core/agent/AgentLoop.java: `reasonNode()` builds `tool_list`, `args_schema`, and `tools_section`; `buildToolSection()`/`buildReturnSection()` are the exact prompt-bloat sources because the fallback branches append `Arguments Details:
  - (none)`, `Return Details:
  - (none)`, and `Examples:
  - (none)`.
- /home/zhixi/GitRepos/ChatMC/base/core/src/main/java/space/controlnet/chatmc/core/tools/AgentTool.java: metadata is currently mandatory by shape (`String` + `List<String>` getters), so sparse tools are represented today by empty lists/blank strings rather than Optional/null.
- /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/tools/McToolProvider.java and /home/zhixi/GitRepos/ChatMC/ext-ae/common-1.20.1/src/main/java/space/controlnet/chatmc/ae/common/tools/AeToolProvider.java: real providers populate full metadata with `List.copyOf(...)`; failure paths consistently return `ToolResult.error(code, message)` and wrap through `ToolOutcome.result(...)`.
- /home/zhixi/GitRepos/ChatMC/base/core/src/main/java/space/controlnet/chatmc/core/tools/ToolMessagePayload.java: chat history payload only serializes `tool`, `thinking`, `args`, `output`, and `error`; the UI/tool-result error field is message-only, so prompt trimming should not rely on error codes.
- /home/zhixi/GitRepos/ChatMC/base/forge-1.20.1/src/main/java/space/controlnet/chatmc/forge/gametest/ServerThreadConfinementGameTest.java: the in-repo sparse tool test double (`StaticAgentTool`) returns empty metadata lists, making it a good regression template for optional-first prompt rendering.
- /home/zhixi/GitRepos/ChatMC/base/core/src/main/java/space/controlnet/chatmc/core/tools/AgentTool.java: adding default `Optional`/normalized-list helpers lets `AgentLoop` consume sparse metadata in an optional-first way without forcing existing mc/ae `AgentTool` implementations to change their legacy `String`/`List<String>` methods.
- /home/zhixi/GitRepos/ChatMC/base/core/src/main/java/space/controlnet/chatmc/core/tools/ToolResult.java: a three-argument `error(payloadJson, code, message)` factory preserves structured failure payloads for MCP-style tool errors while leaving the existing two-argument `error(code, message)` contract untouched for string-sensitive call sites.
- /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/tools/ToolRegistry.java: provider refresh must transfer tool-name ownership on collisions (for example, if provider B overrides a name previously exported by provider A, a later refresh of A must not unregister B's live tool when removing A's stale names).
- /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/test/java/space/controlnet/chatmc/common/tools/ToolRegistryRefreshDeterminismRegressionTest.java: module-scope JUnit workers can retain unrelated static `ToolRegistry` entries from earlier tests, so registry regressions should clean up their own prefix/provider state and assert against owned-name subsets rather than assuming the full snapshot starts empty.
- /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/tools/ToolRegistry.java and /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/agent/AgentRunner.java: keeping affinity lookup as a separate `ToolRegistry.getExecutionAffinity(...)` helper preserves the task-4 boundary rule because `executeTool(...)` stays thread-agnostic while `AgentRunner.McSessionContext` owns the server-thread marshaling policy.
- /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/tools/mcp/McpSchemaMapper.java: projecting MCP tools into a small `McpProjectedTool` record lets `readOnlyHint` survive as diagnostics metadata alongside the derived `AgentTool` without leaking it into prompt text or using it as a registration gate.
- MCP spec page `https://modelcontextprotocol.io/specification/2025-11-25/basic/transports` currently defines stdio framing as UTF-8 JSON-RPC messages delimited by newlines (not `Content-Length` headers), and `stderr` is explicitly non-protocol logging; for ChatMC this makes a bounded per-line reader plus `stderr` discard safe and keeps oversized/malformed stdout failures deterministic.

- Task 6 seam map: mirror `/home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/tools/mcp/transport/McpStdioTransport.java` for the transport contract (`open/request/notify/close`), and `/home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/tools/mcp/McpStdioClientSession.java` for protocol/session orchestration (`open/listTools/callTool/initialize*`); best HTTP additions should stay in the same package split as `McpHttpTransport` under `transport/` plus `McpHttpClientSession` beside the stdio session.
- Repo-wide search found no existing `java.net.http.HttpClient`/`HttpRequest`/`HttpResponse` usage; HTTP-only v1 behavior is already enforced by `/home/zhixi/GitRepos/ChatMC/base/core/src/main/java/space/controlnet/chatmc/core/tools/mcp/McpConfigValidator.java` (`validateHttp`) and the parser/tests around `/home/zhixi/GitRepos/ChatMC/base/core/src/test/java/space/controlnet/chatmc/core/tools/mcp/McpConfigParserValidationRegressionTest.java`.
- Task 6 HTTP transport reminder: request/response-only v1 can stay spec-aligned by POSTing JSON with standard `Accept: application/json, text/event-stream`, capturing optional `MCP-Session-Id` from `initialize`, sending `MCP-Protocol-Version` only after negotiation, and explicitly rejecting `text/event-stream` responses; use `BodyHandlers.ofInputStream()` plus a manual capped read so oversized HTTP bodies fail deterministically instead of buffering unboundedly.
- /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/tools/mcp/McpRuntimeManager.java and McpConfigLoader.java: because `McpConfigLoader.load()` falls back to `McpConfig.defaults()` on missing/invalid files, task-7 reload safety depends on treating `loadedFromDisk=false` as a non-destructive reload when any MCP aliases are already active; otherwise deleting or corrupting `config/chatmc/mcp.json` silently wipes healthy alias providers.

- /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/tools/mcp/McpToolProvider.java and McpFallbackRenderer.java: task-8 stays deterministic when MCP call payloads are normalized to the exact envelope `{serverAlias, qualifiedTool, remoteTool, isError, textContent, structuredContent, unsupportedContentTypes}` and both tool renderers and `ToolOutputFormatter` consume that same envelope with text-first, structured-second fallback rules.
- /home/zhixi/GitRepos/ChatMC/base/common-1.20.1/src/main/java/space/controlnet/chatmc/common/tools/mcp/McpRuntimeManager.java with `McpStdioClientSession`/`McpHttpClientSession`: the real runtime-manager path uses the clients' default 30s request timeout, so fast end-to-end timeout regressions are best driven through the package-private `McpRuntimeManager.SessionFactory` seam while still exercising config load -> runtime discovery -> `McpToolProvider` -> `ToolRegistry.executeTool(...)` end to end.
- F1 plan-compliance auditing is sensitive to the exact JUnit fully qualified class names in the acceptance commands; keeping equivalent assertions under different packages/class names is not enough, so the safest fix is test-scope relocation/renaming that preserves the original regression bodies.
