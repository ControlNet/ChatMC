
- 2026-04-02: Froze the Task 1 contract in `common.tools.http` as separate records: `HttpToolEntry`, `HttpToolRequest`, `HttpToolResponse`, and `HttpToolResultEnvelope`.
- 2026-04-02: Chose `timeoutMs` default `20000` with range `1..120000` and `maxRedirects` default `3` with range `0..5` to make the metadata-facing contract bounded and deterministic.
- 2026-04-02: Used `HttpToolResultEnvelope.KIND = "http_result"` as the structured payload discriminator and named the envelope type explicitly to avoid confusion with core `ToolResult`.
- 2026-04-02: Corrected the drifted Task 1 defaults to the final frozen values from the plan: `timeoutMs` default `10000` with range `1000..25000`, `followRedirects` default `false`, `maxRedirects` default `5` with range `0..10`, and `responseMode` default `auto`.
- 2026-04-02: Final Task 1 envelope shape is `kind + request + response? + failure? + truncated`, with `HttpToolFailure` for local failures and top-level truncation metadata instead of response-level truncation.
- 2026-04-02: Registered the built-in HTTP provider under provider id `http` in `MineAgent.init()` and kept the provider surface to a single exported tool named `http`.
- 2026-04-02: Chose a stable local pre-I/O failure contract for valid HTTP calls: return an `http_result` payload with `failure.code = tool_execution_failed` and message `HTTP tool execution is not implemented.` until real network execution exists.
- 2026-04-02: Used a source-contract assertion for the `MineAgent.java` registration line plus direct `ToolRegistry.registerOrReplace("http", new HttpToolProvider())` in regression tests because full `MineAgent.init()` is not unit-test-safe under the module's Architectury registrar setup.
- 2026-04-02: Built Task 3 around one reusable `HttpToolLoopbackFixture` class with nested response specs (`FixedResponse`, `EchoResponse`, `RedirectResponse`, `RedirectStep`) so downstream tests share one loopback harness instead of proliferating inline `HttpServer` helpers.
- 2026-04-02: The fixture uses a loopback-only `HttpServer`, a dedicated cached executor, idempotent `close()`, and bounded executor shutdown so delayed-response timeout tests stay deterministic and do not strand background handler threads.
- 2026-04-02: Implemented Task 4 with a dedicated package-private `HttpToolRequestPreparation` helper that parses JSON-object arguments, normalizes defaults, appends duplicate-preserving query params onto the effective URL, applies ordered headers to a built `HttpRequest`, and constructs request-body bytes/body-publishers without performing network I/O.
- 2026-04-02: Validation failures that occur after a stable request summary exists now return `ToolResult.error(payloadJson, "invalid_args", message)` with an `http_result` failure envelope, while malformed/non-object JSON still uses the older plain `invalid_args` contract because no normalized request context exists yet.
- 2026-04-02: Task 5 uses a dedicated `HttpToolExecution` helper instead of expanding `HttpToolProvider`, keeping parse/validation concerns separate from runtime HTTP, redirect, and response-decoding behavior.
- 2026-04-02: Completed HTTP exchanges now return `ToolResult.ok(...)` with an `http_result` envelope even for `3xx` (when redirects are disabled) and `4xx/5xx`, while local timeout/redirect-overflow/oversize/decode/runtime failures return payload-preserving `ToolResult.error(...)` envelopes.
- 2026-04-02: Extended `HttpToolResponse` to carry `redirectCount`, parsed `charset`, and `declaredContentLength` so Task 5 can report the final exchange metadata the plan froze without introducing renderer work from Task 6.
- 2026-04-03: Registered a dedicated `HttpToolOutputRenderer` from `MineAgent.init()` and kept its rendering contract intentionally narrow to `kind=http_result`, with failure rendering based on normalized request summary plus `failure.code/message` and success rendering based on final URL plus concise body preview or byte-count metadata.
- 2026-04-03: Kept Task 7 implementation changes test-only by expanding `HttpToolExecutionRegressionTest` with the missing no-body, charset, DNS, truncation, and statelessness cases after the existing executor passed the new matrix unchanged.
- 2026-04-03: Applied the F2 no-body correction in the fixture itself instead of the executor, because the review defect was about loopback test modeling for `HEAD`/`204`/`304`, not the public HTTP tool contract.
- 2026-04-03: Locked the previously uncovered executor branches only through `HttpToolExecutionRegressionTest`: redirect rewrite/preservation behavior, malformed JSON in `responseMode=json`, and unsupported-charset handling for both `json` failure and `auto` fallback.
- 2026-04-03: Documented the HTTP contract in `REPO.md` as a dedicated `16.8` section and locked it with `HttpToolDocumentationContractRegressionTest`, including the exact focused Gradle commands for Tasks 1-8 instead of a broader Task 9 module sweep.
- 2026-04-03: Stored Task 9 evidence as per-class JUnit XML copies for the new HTTP regression classes plus compact XML summaries for the focused package run and the full common-module run under `.sisyphus/evidence/`.
