
- 2026-04-02: Local `lsp_symbols` and `lsp_diagnostics` calls timed out repeatedly during this task, so Gradle compile/test/build results were used as the authoritative verification path.
- 2026-04-02: Initial Task 1 implementation drifted from the plan by omitting `followRedirects` and `responseMode` and by freezing the wrong timeout/redirect defaults; regression assertions were updated so future changes fail fast.
- 2026-04-02: A final Task 1 drift remained in the result envelope: the POST example exceeded the frozen timeout max and the envelope could not represent local failures or top-level truncation until the last correction pass.
- 2026-04-02: Gson record deserialization wrapped `HttpToolRequest` constructor failures in a `RuntimeException`, so `HttpToolProvider` had to unwrap/translate that path to preserve deterministic `invalid_args` messages like `url is required.`
- 2026-04-02: Running the two targeted Gradle test invocations in parallel contended on the shared Fabric Loom cache lock and produced misleading compile noise; sequential runs were stable.
- 2026-04-02: Direct `MineAgent.init()` from a plain JUnit test hit Architectury registrar assertions even after Minecraft bootstrap, so registration coverage had to verify the source contract and registry behavior without full mod initialization.
- 2026-04-02: Local `lsp_diagnostics` initialization timed out again during Task 3 on the new HTTP fixture files, so Gradle test/build results remained the authoritative verification path for this harness change.
- 2026-04-02: Local `lsp_diagnostics` initialization timed out again during Task 4 on the modified HTTP request/provider files, so the targeted HTTP regressions plus `:base:common-1.20.1:build` were used as the authoritative verification evidence.
- 2026-04-02: Local `lsp_diagnostics` initialization timed out again during Task 5 on both the main and test HTTP package directories, so the targeted `HttpToolExecutionRegressionTest` Gradle run remained the authoritative verification signal.
- 2026-04-02: On this JVM, loopback connection refusal did not reliably surface a human-readable `ConnectException` message, so the Task 5 local-error mapper had to inspect the full throwable chain instead of only the root-cause message.
- 2026-04-03: Local `lsp_diagnostics` initialization timed out again during Task 6 on the HTTP renderer and regression test files, so targeted Gradle verification plus `:base:common-1.20.1:build` remained the authoritative validation path.
- 2026-04-03: Success-payload round-trip assertions cannot rely on raw JSON string equality because Gson HTML escaping in the original payload (`=` -> `\u003d`) differs from `JsonElement.toString()` output after `ToolMessagePayload.parse(...)`; semantic JSON equality is the stable compatibility check.
- 2026-04-03: Local `lsp_diagnostics` initialization timed out again during Task 7 on `HttpToolExecutionRegressionTest.java`, so the focused Gradle HTTP suite remained the authoritative verification signal for this regression-matrix pass.
- 2026-04-03: Task 8 hit the same tooling limitation pattern: `.md` has no configured LSP server in this workspace and Java `lsp_diagnostics` initialization timed out again on the new docs regression test, so the targeted Gradle `HttpToolDocumentationContractRegressionTest` run remained the authoritative verification signal.
- 2026-04-03: Local `lsp_diagnostics` initialization timed out again during the F2 correction pass on both `HttpToolLoopbackFixture.java` and `HttpToolExecutionRegressionTest.java`, so the sequential Gradle HTTP/common test runs remained the authoritative verification path.
