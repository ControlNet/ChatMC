
- 2026-04-02: `McToolProvider` style is compact deterministic metadata: one-line schema strings, ordered description lines, and inline JSON example strings.
- 2026-04-02: Common-layer HTTP request/query/header public shapes should stay as ordered `List<HttpToolEntry>` arrays so duplicate entries remain lossless.
- 2026-04-02: The metadata should explicitly remind callers that request bodies must fit under the global tool-args JSON boundary (65536 chars), rather than implying unlimited payload size.
- 2026-04-02: Task 1 contract freeze also needs metadata-visible redirect/decoding controls; `followRedirects` and `responseMode` belong in both the request record and the prompt-facing schema text.
- 2026-04-02: The result envelope contract must carry top-level `truncated` metadata and a separate local `failure` object; keeping truncation only on `response` does not match the plan shape.
- 2026-04-02: Task 2 `HttpToolProvider` can stay minimal by reusing `HttpToolMetadata.spec()`, exposing only `http`, and returning `CALLING_THREAD` so `McSessionContext` bypasses the no-server failure boundary for local pre-I/O handling.
- 2026-04-02: For pre-execution determinism, parsing `argsJson` as a JSON object first and then normalizing through `HttpToolRequest` preserves the frozen validation messages while still returning a local not-implemented failure envelope for otherwise valid requests.
- 2026-04-02: Task 3 loopback coverage is easiest to keep reusable by recording every request per registered path and exposing `awaitRequestCount(...)` plus `lastRecordedExchange(...)`; later HTTP tests can assert request-building behavior without embedding bespoke latches in each test.
- 2026-04-02: `HttpServer` fixture responses need duplicate-preserving header lists instead of maps, and charset/binary defaults are safest as small factory methods (`text`, `binary`, `redirect`) so later tests can express payload mode intent without rebuilding headers manually.
- 2026-04-02: Task 4 request normalization is easiest to keep deterministic by separating raw JSON-object parsing from request preparation, then throwing a payload-carrying validation exception once defaults/query-appending/body decoding have produced a stable request summary.
- 2026-04-02: Building the Java `HttpRequest` on the pre-I/O path already catches invalid request-side state early while still keeping execution stubbed; carrying the normalized `HttpToolRequest` plus request bytes forward makes Task 5 network execution simpler.
- 2026-04-02: Task 5 execution stays deterministic by creating a stateless per-call Java 17 `HttpClient` with `Redirect.NEVER`, then running an explicit redirect loop so `followRedirects`, `maxRedirects`, `finalUrl`, and `redirectCount` come from MineAgent logic rather than JDK policy.
- 2026-04-02: Normalizing response header names to lowercase and sorting by name is the safest way to preserve duplicate values in ordered arrays, because Java `HttpHeaders` does not expose original wire-order across header names.
- 2026-04-02: Forced `responseMode=text|json` should fail locally with `unsupported_response_body` on undecodable binary payloads, while `auto` can fall back to base64 for non-text or undecodable content without turning a completed exchange into a tool failure.
- 2026-04-03: Task 6 HTTP rendering stays deterministic when it uses a fixed line order (`HTTP ...`, `Status`, `Content-Type`, `Truncated`, `Body`) and limits text bodies to a short newline-normalized preview while treating `responseMode=bytes` and `bodyBase64` as metadata-only outputs.
- 2026-04-03: Task 7 confirmed the execution contract already handles `204`, `304`, and `HEAD` as successful no-body exchanges by omitting `bodyText`/`bodyBase64` and reporting `bodyBytes = 0` without any executor changes.
- 2026-04-03: Charset fallback is decoding-only: text responses without a declared charset still decode as UTF-8, but `response.charset` remains absent unless the server explicitly declared one (for example `iso-8859-1`).
- 2026-04-03: Statelessness is best locked by returning `Set-Cookie` from the loopback fixture and asserting repeated invocations never send a `Cookie` request header, proving the per-call `HttpClient` setup does not leak state across tool executions.
- 2026-04-03: Task 8 docs are easiest to keep synchronized by embedding the exact `HttpToolMetadata` args/result schema strings in `REPO.md` and by regression-testing the copied Gradle commands plus implementation constants directly against the markdown.
- 2026-04-03: Task 9 verification stayed clean end-to-end: each HTTP regression class passed individually, the focused package-wide suite passed, and the full `:base:common-1.20.1:test` run stayed green without HTTP-caused drift in formatter ordering or output shape.
- 2026-04-03: Final-wave F2 follow-up showed the loopback fixture should model no-body semantics itself for `HEAD`, `204`, and `304` by sending `-1` length responses instead of relying on the JDK server's warning/correction path.
- 2026-04-03: The redirect regression matrix needs explicit method/body assertions, not just final status assertions: `303` rewrites `POST` to `GET` but preserves `HEAD`, `301/302` rewrite `POST` to `GET`, and `307/308` preserve the original method plus request body.
- 2026-04-03: Unsupported charset coverage is split across two public outcomes: `responseMode=json` must fail with `unsupported_response_body`, while `auto` on a text-like response falls back to `bodyBase64` even when the declared charset is unsupported.
